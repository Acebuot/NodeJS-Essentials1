<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <% for(i=0; i < posts.length; i++) { %>
        <div class="post">
            <h1> <%= posts[i].title %> </h1>
            <p class="meta">By <strong><%= posts[i].author%></strong> on <em><%= posts[i].date %></em></p>
            <p><%= posts[i].post %></p> 
        </div>
    <% } %>
   </body>
</html>
